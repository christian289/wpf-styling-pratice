<Window x:Class="SilentOtter72.Wpf.Gallery.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:SilentOtter72.Wpf.UI.Controls;assembly=SilentOtter72.Wpf.UI"
        mc:Ignorable="d"
        Title="SilentOtter72 Toggle Switch Gallery"
        Height="450" Width="800"
        Background="#F5F5F5">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- 기본 상태 (Unchecked) -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                <TextBlock Text="Unchecked:" VerticalAlignment="Center" Width="100"/>
                <controls:SilentOtter72 IsChecked="False" Margin="20,0,0,0"/>
            </StackPanel>

            <!-- Checked 상태 -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                <TextBlock Text="Checked:" VerticalAlignment="Center" Width="100"/>
                <controls:SilentOtter72 IsChecked="True" Margin="20,0,0,0"/>
            </StackPanel>

            <!-- 인터랙티브 토글 -->
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Interactive:" VerticalAlignment="Center" Width="100"/>
                <controls:SilentOtter72 x:Name="InteractiveToggle" Margin="20,0,0,0"/>
                <TextBlock VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="OFF"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=InteractiveToggle}" Value="True">
                                    <Setter Property="Text" Value="ON"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
