<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:BrightTiger84.Wpf.UI.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/BrightTiger84.Wpf.UI;component/Themes/BrightTiger84Resources.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type controls:BrightTiger84}">
        <Setter Property="Width" Value="{StaticResource BrightTiger84.Size}" />
        <Setter Property="Height" Value="{StaticResource BrightTiger84.Size}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:BrightTiger84}">
                    <Grid Background="{TemplateBinding Background}">
                        <!-- Thumbs Up Icon -->
                        <!-- 좋아요 아이콘
                             Thumbs up icon -->
                        <Path x:Name="ThumbIcon"
                              Data="{StaticResource BrightTiger84.ThumbPath}"
                              Fill="{StaticResource BrightTiger84.Fill.Normal}"
                              Stretch="Uniform"
                              RenderTransformOrigin="0.5,0.5">
                            <Path.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform x:Name="IconScale" ScaleX="1" ScaleY="1" />
                                    <RotateTransform x:Name="IconRotation" Angle="0" />
                                </TransformGroup>
                            </Path.RenderTransform>
                        </Path>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!-- Hover 상태: 확대 및 회전 애니메이션
                             Hover state: scale and rotation animation -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="IconScale"
                                                         Storyboard.TargetProperty="ScaleX"
                                                         To="{StaticResource BrightTiger84.HoverScale}"
                                                         Duration="0:0:0.3" />
                                        <DoubleAnimation Storyboard.TargetName="IconScale"
                                                         Storyboard.TargetProperty="ScaleY"
                                                         To="{StaticResource BrightTiger84.HoverScale}"
                                                         Duration="0:0:0.3" />
                                        <DoubleAnimation Storyboard.TargetName="IconRotation"
                                                         Storyboard.TargetProperty="Angle"
                                                         To="{StaticResource BrightTiger84.HoverRotation}"
                                                         Duration="0:0:0.3" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="IconScale"
                                                         Storyboard.TargetProperty="ScaleX"
                                                         To="1"
                                                         Duration="0:0:0.3" />
                                        <DoubleAnimation Storyboard.TargetName="IconScale"
                                                         Storyboard.TargetProperty="ScaleY"
                                                         To="1"
                                                         Duration="0:0:0.3" />
                                        <DoubleAnimation Storyboard.TargetName="IconRotation"
                                                         Storyboard.TargetProperty="Angle"
                                                         To="0"
                                                         Duration="0:0:0.3" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <!-- Checked 상태: 파란색으로 변경
                             Checked state: change to blue -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ThumbIcon" Property="Fill" Value="{StaticResource BrightTiger84.Fill.Checked}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
