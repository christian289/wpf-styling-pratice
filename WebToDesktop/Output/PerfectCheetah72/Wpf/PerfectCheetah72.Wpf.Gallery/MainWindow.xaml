<Window x:Class="PerfectCheetah72.Wpf.Gallery.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:PerfectCheetah72.Wpf.UI.Controls;assembly=PerfectCheetah72.Wpf.UI"
        mc:Ignorable="d"
        Title="PerfectCheetah72 - Theme Selector Gallery"
        Height="450" Width="800"
        Background="#1E1E1E">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Theme Selector Control"
                       Foreground="#E0E0E0"
                       FontSize="24"
                       FontWeight="Bold"
                       Margin="0,0,0,30"
                       HorizontalAlignment="Center"/>

            <controls:PerfectCheetah72 x:Name="ThemeSelector"
                                       ButtonText="Theme"
                                       SelectedTheme="Default"/>

            <TextBlock x:Name="StatusText"
                       Foreground="#A0A0A0"
                       FontSize="14"
                       Margin="0,30,0,0"
                       HorizontalAlignment="Center">
                <Run Text="Selected Theme: "/>
                <Run Text="{Binding SelectedTheme, ElementName=ThemeSelector}"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
