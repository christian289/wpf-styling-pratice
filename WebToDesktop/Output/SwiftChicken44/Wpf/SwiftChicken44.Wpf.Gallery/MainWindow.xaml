<Window x:Class="SwiftChicken44.Wpf.Gallery.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:SwiftChicken44.Wpf.UI.Controls;assembly=SwiftChicken44.Wpf.UI"
        mc:Ignorable="d"
        Title="SwiftChicken44 Gallery" Height="450" Width="800"
        Background="#1a1a2e">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- Default State (IsValid = null) -->
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Default State (IsValid = null)" Foreground="White" Margin="0,0,0,5"/>
                <controls:SwiftChicken44 Width="250"/>
            </StackPanel>

            <!-- Valid State -->
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Valid State (IsValid = true)" Foreground="#00FF2A" Margin="0,0,0,5"/>
                <controls:SwiftChicken44 Width="250" IsValid="True" Text="Valid Input"/>
            </StackPanel>

            <!-- Invalid State -->
            <StackPanel>
                <TextBlock Text="Invalid State (IsValid = false)" Foreground="#FF0000" Margin="0,0,0,5"/>
                <controls:SwiftChicken44 Width="250" IsValid="False" Text="Invalid Input"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
