<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:BigSloth59.Wpf.UI.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/BigSloth59.Wpf.UI;component/Themes/BigSloth59Resources.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type controls:BigSloth59}">
        <Setter Property="Height" Value="{StaticResource BigSloth59.Height}" />
        <Setter Property="FontSize" Value="{StaticResource BigSloth59.FontSize}" />
        <Setter Property="FontWeight" Value="{StaticResource BigSloth59.FontWeight}" />
        <Setter Property="Background" Value="{StaticResource BigSloth59.Background}" />
        <Setter Property="Foreground" Value="{StaticResource BigSloth59.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource BigSloth59.BorderBrush.Normal}" />
        <Setter Property="BorderThickness" Value="{StaticResource BigSloth59.BorderThickness}" />
        <Setter Property="Padding" Value="{StaticResource BigSloth59.Padding}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Cursor" Value="IBeam" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:BigSloth59}">
                    <Grid>
                        <!-- Main Border with rounded corners and bottom border -->
                        <Border x:Name="PART_Border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource BigSloth59.CornerRadius}"
                                SnapsToDevicePixels="True">
                            <Border.BorderBrush>
                                <SolidColorBrush x:Name="BorderBrushAnim" Color="#e5e5e5" />
                            </Border.BorderBrush>
                            <Grid>
                                <!-- Placeholder Text -->
                                <TextBlock x:Name="PART_Placeholder"
                                           Text="Type..."
                                           Foreground="{StaticResource BigSloth59.Placeholder.Foreground}"
                                           VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                           HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                           Margin="{TemplateBinding Padding}"
                                           IsHitTestVisible="False"
                                           Visibility="Visible" />
                                <!-- Content Host for TextBox -->
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Focusable="False"
                                              HorizontalScrollBarVisibility="Hidden"
                                              VerticalScrollBarVisibility="Hidden"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}" />
                            </Grid>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Hide placeholder when text is entered -->
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="PART_Placeholder" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <DataTrigger Binding="{Binding Text.Length, RelativeSource={RelativeSource Self}}" Value="0">
                            <Setter TargetName="PART_Placeholder" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                        <Trigger Property="Text" Value="{x:Null}">
                            <Setter TargetName="PART_Placeholder" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <!-- Hide placeholder when there is text -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Text" Value="" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_Placeholder" Property="Visibility" Value="Visible" />
                        </MultiTrigger>
                        <!-- Focus state with animation -->
                        <Trigger Property="IsFocused" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- transition: 0.5s - border color animation -->
                                        <ColorAnimation Storyboard.TargetName="BorderBrushAnim"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#6941c6"
                                                        Duration="0:0:0.5" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BorderBrushAnim"
                                                        Storyboard.TargetProperty="Color"
                                                        To="#e5e5e5"
                                                        Duration="0:0:0.5" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <!-- Hide placeholder when text exists -->
        <Style.Triggers>
            <Trigger Property="Text" Value="">
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
